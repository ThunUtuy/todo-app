{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A todo app with authentication that supports CRUD operations",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:e2e" : "docker-compose -f docker-compose.yml -f docker-compose.test.yml up --build --abort-on-container-exit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
